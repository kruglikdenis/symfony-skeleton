oneup_flysystem:
    adapters:
        local_adapter:
            local:
                directory: '/web/files'
                lazy: ~
                writeFlags: ~
                linkHandling: ~
                permissions: ~

    filesystems:
        local:
            adapter: local_adapter
            alias: local
